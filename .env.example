TOGGL_EMAIL=
TOGGL_PASSWORD=

# Substack signup configuration
SUBSTACK_PUBLICATION_URL=    # Full public URL of your Substack publication
SUBSTACK_ORIGIN=             # Optional origin header override (defaults to publication URL)
SUBSTACK_REFERER=            # Optional referer header override (defaults to origin)
SUBSTACK_USER_AGENT=         # Custom User-Agent string for subscription requests
SUBSTACK_REFERRING_DOMAIN=   # Optional domain metadata passed to Substack
SUBSTACK_REFERRING_URL=      # Optional URL metadata passed to Substack
SUBSTACK_SOURCE=             # Optional source tag for Substack analytics
SUBSTACK_PUBLICATION_ID=     # Optional numeric publication ID if required by Substack
SUBSTACK_ENDPOINT_PATH=      # Optional override path (defaults to /api/v1/free)
SUBSTACK_INCLUDE_NOJS=       # Set to false to omit ?nojs=true when hitting /api/v1/free

# HereNowFyi Widget Configuration
NEXT_PUBLIC_HERENOW_BASE_URL=https://www.herenow.fyi
